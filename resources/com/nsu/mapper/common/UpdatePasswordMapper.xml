<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nsu.dao.common.UpdatePasswordDao">
	
	<select id="getPasswordById" parameterType="int" resultType="String" >
		SELECT A_PASSWORD FROM ACCOUNT where A_ID = #{id} 
	</select>
	
	<update id="updatePassword" parameterType="java.util.HashMap">
		UPDATE  ACCOUNT SET	 A_PASSWORD = #{PASSWORD},A_SALT = #{SALT}  WHERE A_ID = #{A_ID}
	</update>
</mapper>