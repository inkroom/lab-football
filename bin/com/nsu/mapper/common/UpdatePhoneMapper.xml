<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nsu.dao.common.UpdatePhoneDao">
	
	<select id="getPhoneById" parameterType="int" resultType="String" >
		SELECT A_PHONE FROM ACCOUNT where A_ID = #{id} 
	</select>
	
	<update id="updatePhone" parameterType="java.util.HashMap">
		UPDATE  ACCOUNT SET	 A_PHONE = #{PHONE}, A_USERNAME=#{PHONE} WHERE A_ID = #{A_ID}
	</update>
	
	<update id="updatePhoneLive" parameterType="java.util.HashMap">
		UPDATE  ACCOUNT SET	 A_PHONE = #{PHONE}  WHERE A_ID = #{A_ID}
	</update>
</mapper>