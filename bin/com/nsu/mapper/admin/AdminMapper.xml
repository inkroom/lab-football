<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nsu.dao.admin.AdminDao">

	<select id="getUserByUsername" resultType="UserBean">
		SELECT A_ID as id,A_USERNAME AS userName,A_PASSWORD AS password,A_TYPE as type FROM ACCOUNT WHERE A_USERNAME = #{userName} AND A_STATUS = 1 AND A_TYPE = 0;
	</select>

</mapper>